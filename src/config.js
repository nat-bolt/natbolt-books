// ── App-wide constants ────────────────────────────────────────────────────────
// Single source of truth for plan limits and other shared config values.

/** Maximum bills per month on the free plan. */
export const FREE_BILL_LIMIT = 30;
